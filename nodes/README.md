# 内置节点

这里预留通过 `sdk` 公共接口实现的内置节点。算法依赖通过 `third_party` 引入，并由实际使用它们的节点目标链接。

当前没有节点实现或构建目标。新增实现时再建立对应子目录并更新本目录的 `CMakeLists.txt`；不提前创建算法分类目录。

节点计算逻辑不依赖 `editor`。未来自定义节点界面作为可选部分单独组织，节点注册与执行由应用装配及 `managers` 协作完成。
